<workflow-app name="${appName}-pp" xmlns="uri:oozie:workflow:0.5">
    <global>
        <configuration>
            <property>
                <name>oozie.launcher.mapred.job.queue.name</name>
                <value>${cbsOozieLauncher}</value>
            </property>
            <property>
                <name>mapreduce.job.queuename</name>
                <value>${queue}</value>
            </property>
            <property>
                <name>tez.queue.name</name>
                <value>${queue}</value>
            </property>
        </configuration>
    </global>

    ${pom.businessviews.xml.hivecredential.bloc_comment_begin}
    <credentials>
        <credential name='hive_cred' type='hcat'>
            <property>
                <name>hcat.metastore.uri</name>
                <value>${kerberosHiveMetastoreUri}</value>
            </property>
            <property>
                <name>hcat.metastore.principal</name>
                <value>${kerberosHivePrincipal}</value>
            </property>
        </credential>
    </credentials>
    ${pom.businessviews.xml.hivecredential.bloc_comment_end}

    <start to="build_tiers_pp_process"/>

    <action name="build_tiers_pp_process">
        <shell xmlns="uri:oozie:shell-action:0.3">
            <job-tracker>${resourceManager}</job-tracker>
            <name-node>${nameNode}</name-node>
            <prepare>
                <delete path="${hdfsDataOutputLocation}/pp_bv/${partitionId}"/>
            </prepare>
            <exec>run-bt.sh</exec>
            <argument>buildPP</argument>
            <env-var>kerberosUser=${kerberosUser}</env-var>
            <env-var>kerberosPrincipal=${kerberosPrincipal}</env-var>
            <env-var>master=${master}</env-var>
            <env-var>deploy=${deploy}</env-var>
            <env-var>queue=${queue}</env-var>
            <env-var>HADOOP_USER_NAME=${wf:user()}</env-var>
            <file>${kerberosAppKeytabPath}</file>
            <file>${businessviewsRelPathSparksubmitSh}</file>
            <capture-output/>
        </shell>
        <ok to="kafka_producer_pp_process"/>
        <error to="kill"/>
    </action>

    <action name="kafka_producer_pp_process">
        <shell xmlns="uri:oozie:shell-action:0.3">
            <job-tracker>${resourceManager}</job-tracker>
            <name-node>${nameNode}</name-node>
            <exec>run-producer.sh</exec>
            <argument>pp</argument>
            <env-var>kerberosUser=${kerberosUser}</env-var>
            <env-var>kerberosPrincipal=${kerberosPrincipal}</env-var>
            <env-var>master=${master}</env-var>
            <env-var>deploy=${deploy}</env-var>
            <env-var>queue=${queue}</env-var>
            <env-var>HADOOP_USER_NAME=${wf:user()}</env-var>
            <file>${kerberosAppKeytabPath}</file>
            <file>${kafkaProdRelPathKafkaProd}/lib</file>
            <file>${kafkaProdRelPathKafkaProd}/etc</file>
            <file>${kafkaProdRelPathKafkaProd}/bin</file>
            <file>${kafkaProdRelPathSparksubmitSh}</file>
            <capture-output/>
        </shell>
        <ok to="end"/>
        <error to="kill"/>
    </action>

    <kill name="kill">
        <message>Action failed, error message[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>
    <end name="end"/>

</workflow-app>